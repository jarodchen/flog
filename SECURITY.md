# 安全策略

## 支持的版本

本项目是一个静态站点，始终以 `main` 分支的最新代码为准。
安全修复只针对最新版本发布。

| 版本 | 是否支持 |
| --- | --- |
| `main`（最新） | ✅ 支持 |
| 历史标签 | ❌ 不支持 |

## 报告漏洞

**请不要通过公开 Issue 报告安全漏洞。**

请改用以下私密渠道：

1. 打开[新建私密安全公告](https://github.com/jarodchen/flog/security/advisories/new)页面；
2. 填写漏洞描述后提交。

只有仓库维护者能看到内容，在修复发布前不会公开。

### 请在报告中包含

- 漏洞类型（例如跨站脚本、依赖项漏洞、敏感信息泄露）
- 受影响的文件路径或页面地址
- 复现步骤，最好能给出最小复现示例
- 潜在影响的评估
- 如果有，修复建议

信息越完整，定位和修复越快。

### 响应流程

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到报告 | 3 个工作日内 |
| 初步评估并反馈结论 | 7 个工作日内 |
| 发布修复 | 视严重程度而定，高危问题优先处理 |

修复发布后，如果你愿意，我们会在安全公告中致谢。

## 本项目的安全边界

这是一个纯静态站点，没有后端服务、没有数据库、不收集用户数据、不处理登录态。
因此以下这类问题通常**不在**本项目范围内：

- 部署平台（GitHub Pages）自身的问题，请报告给 GitHub
- 需要攻击者已控制维护者本地环境才能触发的问题
- 仅影响开发依赖、且不影响构建产物的告警
- 缺少某些安全响应头 —— 静态托管平台的响应头不由本仓库控制

以下问题**在**范围内：

- 构建产物中被注入的恶意脚本
- 文章或照片元信息渲染时导致的跨站脚本
- 构建脚本中的路径穿越或任意文件读写
- 仓库中意外提交的密钥、令牌等敏感信息

## 依赖安全

- CI 每周会跑一次依赖与质量检查，见 [ci.yml](.github/workflows/ci.yml)
- Pull Request 会触发依赖审查，中危及以上会被拦截
- 如果你发现某个依赖存在已知漏洞，欢迎直接提 PR 升级

## 如果你发现仓库里有泄露的密钥

请立刻通过上面的私密渠道告知，不要在公开场合提及具体内容。
